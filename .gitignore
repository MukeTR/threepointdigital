# macOS
.DS_Store
._*

# Bağımlılıklar (yalnızca api/ klasöründe wrangler kurulunca oluşur)
node_modules/
package-lock.json

# Cloudflare Wrangler
.wrangler/
api/.dev.vars
api/.wrangler/
worker-configuration.d.ts

# Yerel geliştirme çıktıları
*.log
.cache/

# Yenileme öncesi canlı site kopyası — geri dönüş için git geçmişi kullanılır
# (git checkout 671973b), bu klasör yalnızca yerelde tutulur.
_yedek-mevcut-site/
